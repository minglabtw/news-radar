# 02-工作日誌.md — 專案開發日誌

> 遵循 `AGENTS.md` 規範，每份文件核心條目上限 15 條。本文件已於 2026-05-28 完成高密度提煉與精簡，目前包含 **11 條核心條目**，完美符合合規限制。

---

## 2026-05-28 (V5.0 - V5.3 全中文重構與總編輯優化)

- **【條目 11】V5.3 贅字正則修復與 Execution 549 驗證**：修復了 `node-008-build` 的 JS 解析代碼在 n8n JSON 中雙反斜線 `\\s` 轉義失效而退回預設值的 bug，改用 `new RegExp("^[\\\\s\\\\S]*...")` 構造器成功避開轉義地獄；同時更新總編輯 Prompt 為寬鬆引導。部署更新後 Execution 549 順利跑完，經 GitHub API 與頁面確認已 100% 徹底剪除重複的【今日總結】與 Briefing 裝飾性標題，排版重歸完美。
- **【條目 10】V5.1 - V5.2 README 中文化與修復 MiniMax 空回應 Bug**：將 max_tokens 提高至 16384 以防推理模型思考耗盡；將 README.md 改為全繁體中文，消除不必要英文術語。
- **【條目 9】V5.0 全中文聚焦版改版與 RSS 來源重構**：移除 30 個英文來源，重構為 5 大中文領域 16 個來源；簡化 MiniMax Prompt 為「用繁體中文重新精煉標題，符合台灣用語習慣，請勿計算字數」，防範推理模型計算字數崩潰；移除了 5 個多餘 Set 節點，精簡畫布。
- **【條目 8】V5.0 部署腳本與文件重構**：在 `deploy_and_run.js` 中加入自動 `delete wfData.versionId` 防部分更新漏洞；新撰寫 `06-HANDOVER.md` 交接手冊，重構 00 到 07 號引導文件。

---

## 2026-05-26 (V4.0 - V4.5 雙通道分流、LINE 推送與 GitHub Actions Pages 部署)

- **【條目 7】引入 .nojekyll 繞過 Jekyll 部署與 Pages 服務重設**：向倉庫推送 `.nojekyll` 檔案以徹底避免 JS 雙大括號與 Jekyll 語法衝突；利用 API 重啟 Pages 站點解決網頁卡死 `"errored"` 漏洞。
- **【條目 6】V4.5 雙通道分流串聯修復與領域錯位根治**：引入 `item.pairedItem[0].item` 機制回溯來源索引，徹底根治了抓取超時導致的領域數據錯位問題。
- **【條目 5】V4.5 全動態泛領域適應、過刊 SHA 對接與前台互動升級**：前台 HTML 實作動態 Chip 按鈕過濾、搜尋，配置 10 大主題配色；開發 `取得archive SHA` 節點解決過刊覆寫 422 錯誤。
- **【條目 4】V4.0 雙平台 LINE 推送與視覺開關重構**：對接 LINE Messaging API，新增無 HTML 標籤的 `lineText` 純文字推送防報錯；新增並排 Set 節點，實作在畫布上一鍵點擊視覺化啟用/停用特定的新聞領域。
- **【條目 3】修復排程未按時啟動之 API 部署時區覆寫漏洞**：在 `workflow-native-v3.json` 的 `"settings"` 屬性中強制寫入 `"timezone": "Asia/Taipei"`，避免 API 部署將時區重置為紐約。

---

## 2026-05-24 至 2026-05-25 (CI/CD 覆寫與 V3 穩定版重構)

- **【條目 2】重構為固定 ID 原地熱更新 (PUT) 工業級 CI/CD SOP**：重構部署腳本為 `PUT /api/v1/workflows/{id}` 原地覆寫，固定工作流 ID 為 `1TMNv2FvxSsBAjrm`。解決了 DELETE+POST 導致排程定時器失效的隱患，並完整保留歷史日誌。
- **【條目 1】安全配置分離、網頁女聲播報與 Logo 品牌化**：將 API 金鑰等敏感數據抽離至 `src/config.json`；前端升級 window.speechSynthesis 經典女聲「美佳」播報；Telegram 實作隱形連結技術在推播頂部展現報紙風格預覽圖。
