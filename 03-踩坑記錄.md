# 03-踩坑記錄.md — V5.0 專案排錯與避坑指南

> 遵循 `AGENTS.md` 規範，每份文件核心條目上限 15 條。本文件已於 2026-05-28 完成重組，目前包含 **15 條核心踩坑條目**，符合合規限制。

---

### 1. n8n HTTP / JSON Body 參數與表達式配置坑
- **症狀**: 使用 `body` + `contentType: "json"` 匯入成功但未送出 Body；或者 `jsonBody: "={{ $json.var }}"` 執行時報 "not valid JSON"。
- **根因**:
   1. n8n v4.2 必須使用 `sendBody: true`, `specifyBody: "json"`, `jsonBody: "..."` 參數組合，舊版 `body` 參數會被系統忽略。
   2. `jsonBody` 欄位在 n8n 底層屬於字串型，只接受模板替換 `{{ }}`，**不接受以 `=` 開頭的表達式**，否則會被當作字串進行 JSON 解析而報錯。
- **解法**: 全面將 HTTP Request 節點參數修正為 `specifyBody: "json"` 並移除等號改用 `jsonBody: "{{ $json.mmBody }}"`。

---

### 2. n8n 節點類型與 executeCommand 禁用坑
- **症狀**: 連線到 `rssfeedread` 節點時連線消失；工作流啟用時報 "Unrecognized node type: executeCommand"。
- **根因**:
   1. `rssfeedread` 是觸發節點 (Trigger)，無法被其他節點 (如 Schedule) 觸發。
   2. n8n 雲端伺服器出於安全考量禁用了 `executeCommand` 系統指令節點。
- **解法**: 改用原生 `httpRequest` GET 節點手動抓取 RSS XML；全面改用原生 HTTP/Telegram/GitHub 節點，實現 100% 雲端全原生部署。

---

### 3. AI 請求超時與推理模型 Token 耗盡坑 (MiniMax-M2.7)
- **症狀**: 呼叫 MiniMax LLM API 時出現 `The connection was aborted`，或者回應為空白字串、整個節點超時。
- **根因**:
   1. LLM 產生長文本耗時經常超過 n8n 預設的 60 秒連線 Timeout。
   2. M2.7 推理模型會進行龐大的內部思維鏈 (Thinking Process)，若提示詞包含嚴格的字數限制 (如「限30字內」)，模型會陷入反覆字數計算的死循環，在產出真正的 text 前就因為觸發 `max_tokens` 上限而崩潰。
- **解法**:
   1. 在 HTTP 節點的 `options` 中將 `timeout` 提高到 `300000` (5分鐘)。
   2. 將 `max_tokens` 參數提高到 **8192**，並**拔除 Prompt 中所有嚴格字數限制**，改為寬鬆語氣 (如「簡短且洗鍊地翻譯」)。

---

### 4. AI 格式隨機變異與嚴格 Regex 解析崩潰坑
- **症狀**: 網頁新聞卡片內容全部變成 "最新消息" 等預設 fallback 內容，無法顯示 AI 分析的摘要與標籤。
- **根因**: AI 輸出的文字往往包含不可控的 Markdown 符號 (如 `**摘要：**` 或 `- 摘要:`) 或是混用半半形冒號、換行等。若在 Code 節點中使用嚴格的 Regex (如 `摘要：(.*?)`) 會導致匹配全數失效而退回預設值。
- **解法**:
   1. 在正則匹配前先進行資料清洗，使用 `response.replace(/[*#-]/g, "")` 清除干擾符號。
   2. 使用寬容的正則或 Chunk Parser，如 `split(/(?:摘要|Summary)[^:：\n]*[:：]?\s*/i)` 切割字串，確保高容錯。

---

### 5. 跨節點取值與 `$json` 變數覆蓋坑
- **症狀**: 跨節點取值時，變數拿到 `undefined`，或者送出非預期資料。
- **根因**: 在 n8n 中，當前的 `$json` 會被上游最近一個節點的輸出覆蓋。若直接使用 `$json.var` 會因為上游節點 (如發送 HTTP 請求後) 變更而失效。
- **解法**: 跨節點取值時，必須精準指定來源節點的名稱：`={{ $('產出資料的節點').first().json.var }}`。

---

### 6. Telegram `<b>` HTML 未渲染與訊息默默丟包坑
- **症狀**: Telegram 推播中的 HTML 標籤顯示為純文字，或者部分日報訊息「完全收不到」但 n8n 卻沒有報錯。
- **根因**:
   1. Telegram 節點頂層的 `parseMode` 參數不生效，必須放在 `additionalFields` 的 `parse_mode` 中。
   2. 如果選用 Markdown，一旦摘要中含有未閉合的特殊符號 (如單個 `*` 或 `_`)，Telegram 伺服器會判定為語法錯誤並直接丟棄訊息；在 HTML 模式下若未對 `<`、`>`、`&` 等保留字轉義，一樣會被丟包。
- **解法**: 在 `additionalFields` 中指定 `parse_mode: "HTML"`。在 Code 節點中組裝 `tgText` 時，實作轉義函數：`function esc(s) { return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }`。

---

### 7. 自動排程 (Schedule Trigger) 未按時觸發時區坑
- **症狀**: 手動執行完全正常，但早晨 8 點定時排程卻未啟動 (在半夜或晚上錯亂觸發)。
- **根因**: n8n 伺服器預設時區為紐約。每次使用 `deploy_and_run.js` API 部署時，會將工作流在伺服器上覆寫重建，如果本地 JSON 的 `"settings"` 中沒有聲明時區，伺服器會將其自動重設為紐約時區，覆蓋手動 UI 設定。
- **解法**: 在 `workflow-native-v3.json` 結尾的 `"settings"` 屬性中，強制寫入 `"timezone": "Asia/Taipei"`，確保每次部署時時區不跑掉。

---

### 8. JSON 轉義字元與 Shell 雙引號衝突系列坑
- **症狀**: 
   1. 手工編輯 JSON 內部 JS 程式碼時，沒有正確處理跳脫字元，將 `\n`、`\t` 寫成實體換行，導致 JSON 解析與大括號閉合破壞。
   2. 在 Shell 命令行執行 `node -e "code.replace('\\\\n', '...')"` 時，Shell 雙引號預先吃掉反斜線，導致 Node 收到比預期少一半的反斜線，replace 找不到目標。
- **解法**: 
   1. 複雜的 JSON 操作應使用獨立的 `.js` 腳本檔案執行，避免使用命令行的 `node -e "..."`。
   2. 修改包含跳脫字元的 JS 區塊時，使用 `JSON.stringify` 進行程式化安全轉換，並在匯出前通過嚴格的 JSON Validator 檢測。

---

### 9. API 自動部署時 Webhook 觸發路由未註冊坑
- **症狀**: 用 API 原地更新並啟用工作流後，立刻打 Webhook 觸發，收到 200/404，但 n8n 內部完全沒有 Execution 紀錄。
- **根因**: n8n 系統在接收到啟動指令後，需要大約數秒鐘的時間將 Webhook 路由註冊至底層 Server。手速過快會導致 Webhook 路由尚未註冊完成，請求被默默丟失。
- **解法**: 在部署腳本中，於 `activate` API 和觸發 Webhook API 之間，寫入 `await new Promise(r => setTimeout(r, 6000));` 延遲，並設計 3 次 Webhook 重試循環。

---

### 10. Code 節點 typeVersion 2 不支援多 Branch 輸出坑
- **症狀**: 在 connections 中為 Code 節點 (v2) 新增第二條 Branch index 1，部署後執行時報 "Node has no branch with index 1" 嚴重錯誤。
- **根因**: n8n Code 節點 `typeVersion: 2` 預設只有單一 main 輸出。若要在 connections 中使用多 Branch 並行連線，必須在節點 JSON 中加入 `outputs: ["main", "main"]` 宣告，否則連線無效。
- **解法**: 放棄多 Branch 連線，改為串聯模式。下游節點直接透過 `$('上游節點名稱').first().json.fieldName` 讀取所需資料。

---

### 11. 多領域 RSS 抓取失敗引發的陣列索引錯位坑
- **症狀**: 啟用全部 10 領域、40 個 RSS 來源，執行成功但最終 HTML 僅產出 Tech 文章，其餘 9 領域全部消失。
- **根因**: 原代碼在 `解析RSS+建Prompt` 節點中以 `sourceItems[i]` 索引對應抓取回來的 `inputItems[i]`。當部分 RSS 連線超時失敗時，`inputItems` 長度會小於 `sourceItems`，導致索引全面便宜，所有文章都被錯誤地配對到了 Tech 領域。
- **解法**: 在 Code 節點中引入 `item.pairedItem[0].item` 機制，精確回溯來源索引，或者改用 URL / name 做 key 配對，徹底解決索引偏移 Bug。

---

### 12. LINE Push 通知因 HTML 標籤解析報錯坑
- **症狀**: LINE Push 通知一次都沒有成功發送過，LINE 伺服器報錯。
- **根因**: LINE Messaging API 的 `push` / `broadcast` 端點只接受純文字 (Plain Text)，如果將包含 HTML 標籤 (如 `<b>`、`<br>`) 的日報內容直接送出，LINE 伺服器會解析失敗並直接拒絕發送。
- **解法**: Code 節點中專門為 LINE 輸出純淨的 `lineText` 變數 (使用 JS 正則將所有 HTML 標籤與特殊轉義實體 `&lt;` 等徹底清除乾淨)，確保 LINE 原生推播 100% 成功。

---

### 13. Jekyll 液態語法解析衝突引發 GitHub Pages 部署卡死 errored 坑
- **症狀**: n8n 工作流顯示執行成功、GitHub 上的 index.html 代碼也是最新的 80+ 篇新聞，但 live 網頁上一直顯示舊日報或回傳 404，Pages 狀態在 API 中卡死在 `"errored"`。
- **根因**: GitHub 倉庫中**缺少 `.nojekyll` 檔案**。這導致 GitHub 預設會調用 Jekyll 引擎來編譯 index.html。而我們的 index.html 為了實作前端 CSS 配色與 Web Speech API 語音朗讀，包含了複雜的 JS 代碼與雙大括號 `{ {` 字符序列，被 Jekyll 誤判為 Liquid 液態模板語法而編譯崩潰。崩潰後 Pages 伺服器會卡死在錯誤狀態，拒絕響應後續的所有 Git Push。
- **解法**:
   1. 往 GitHub 倉庫根目錄手動或程式化上傳一個空 `.nojekyll` 檔案，強制跳過 Jekyll 編譯。
   2. 若 Pages webhook 卡死，引導使用者在 GitHub 倉庫的 `Settings → Pages` 下，手動切換 Branch 為 `None` 點擊 **Save**，隨後再切換回 `main` 點擊 **Save**。這會強制重置 Pages 的 webhook 路由並拉取最新代碼，使網頁在 10 秒內直接 active 發佈成功！

---

### 14. V5.0 踩坑：n8n API 401 Unauthorized 認證失效
- **日期**: 2026-05-28
- **症狀**: `deploy_and_run.js` 部署時 n8n 伺服器回傳 `401 unauthorized`，無法更新工作流。
- **根因**: `n8n_api_key` 已失效或過期。
- **解法**:
    1. 前往 `https://n8n.sunder.net.tw` → Settings → API Keys
    2. 檢查目前 API Key 狀態，若顯示 `inactive` 或已過期，請**新建**一個 API Key
    3. 將新 Key 填入 `src/config.json` 的 `n8n_api_key`
    4. 重新執行 `node src/deploy_and_run.js`
- **如何避免**: 定期檢查 API Key 有效期 (`"exp"` 欄位)，在過期前更新。

---

### 15. V5.3 新踩坑：n8n JSON 中 RegExp 雙重轉義失效坑
- **日期**: 2026-05-28
- **症狀**: 在 n8n Code 節點中使用 JS 正則表達式字面量如 `/^[\s\S]*/`，匯入 n8n JSON 後，`\s`（代表空白/換行）會被轉義為 `\\s`。在 JS 引擎執行時，`\\s` 會被誤判為「斜線與字元 s」，導致正則完全匹配失效，被迫退回預設 substring 擷取，網頁上因而頑固地出現重複標題與 `【今日總結】` 等贅字。
- **解法**: 捨棄正則字面量表示法，改用 `new RegExp` 建構子來避開 JSON 轉義地獄。在 JSON 中寫入四個反斜線 `new RegExp("^[\\\\s\\\\S]*...")`，以便在 n8n 執行期能正確編譯出預期的正則。
- **如何避免**: 在 n8n 的 JSON 中撰寫任何涉及反斜線的正則表達式時，優先使用 `new RegExp` 並補足四個反斜線（`\\\\`）進行轉義。

---

> 最後更新: 2026-05-28
> 版本: V5.0 全中文聚焦版
> 踩坑總數: 15 條 (符合 15 條上限)
